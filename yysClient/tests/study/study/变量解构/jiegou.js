// 1. 数组的解构赋值 § ⇧
let [a, b, c] = [1, 2, 3];
console.log(a,b,c)

//2. 对象结构给值
let {foo, bar} = {foo: 'aaa', bar: 'bbb'}

console.log(foo,bar)

//3 .字符串的解构赋值
const [a1, b1, c1, d, e] = 'hello';
console.log(a1,b1,c1,d,e)
